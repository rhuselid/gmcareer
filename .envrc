# Direnv: auto-activate project env when you cd into GMCareer.
# Run once: direnv allow
# Requires: https://direnv.net/ (and on Windows, often used with WSL or Git Bash)

layout python
# Install deps on first enter (idempotent)
[ -f .direnv/pip-done ] || (pip install -r requirements.txt && mkdir -p .direnv && touch .direnv/pip-done)
